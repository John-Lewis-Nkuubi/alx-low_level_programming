#include <stdio.h>

/**
 * main - Program that prints lowercase of the aphabet
 * return - (0) success
 */

int main(void)
{
	char n = 'a';

	if(n <= 'z')
	{
		putchar (n);
		putchar ('\n');
	}
	return (0);
}
