#include <stdio.h>

/**
 * main - program that prints lowercase alphabet in reverse
 * Return: 0 (success)
 */

int main(void)
{
	int h;

	for (h = 'z'; h <= 'z'; h++)
		putchar (h);
	putchar ('\n');
	return (0);
}
