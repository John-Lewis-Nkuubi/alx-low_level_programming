#include <stdio.h>

/**
 * main - program that prints all single digit numbers of base 10
 * Return: 0 (success)
 */

int main(void)
{
	int q;

	for (q = 0; q < 10; q++)
		putchar (q);
	putchar ('\n');
	return (0);
}
