#include "main.h"

/**
 * add - a function that adds two 
 * integers and returns the result
 * @: the frist integer
 * @b: the second integer
 * Return: 0
 */

int add(int a, int b);
{
	int result = a + b;

	return (result);
}
